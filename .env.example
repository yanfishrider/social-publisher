# ============================================================================
# 社交平台发布工具 — 环境变量配置
# ============================================================================
# 使用方法：
#   1. 复制为 .env：cp .env.example .env
#   2. 填入你的真实凭据
#   3. .env 文件已加入 .gitignore，不会被提交到 git
# ============================================================================

# 小红书 — 浏览器自动化，不需要 API 密钥
# 只需在 chromium-browser-data 中保存登录态即可（见 README）

# 搜狐号 — 浏览器自动化，不需要 API 密钥

# 百家号 — 浏览器自动化，不需要 API 密钥

# 今日头条 — 浏览器自动化，不需要 API 密钥
